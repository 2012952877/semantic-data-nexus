# Security Policy

## 报告安全问题

请不要为疑似漏洞创建公开 Issue，也不要附加凭据、真实数据或可利用细节。使用 GitHub 仓库的 **Security advisories -> Report a vulnerability** 私密报告渠道；若该功能不可用，请联系仓库所有者建立私密沟通渠道。

报告应尽量包含：受影响版本/提交、最小复现、潜在影响和建议缓解。请使用合成数据并删除令牌、连接串和内部 URL。

## 当前支持范围

仓库处于 P0，尚无生产服务或 Azure 部署。仅默认分支最新版本接受修复；历史实验提交不提供安全支持。

## 安全不变量

- 不直接执行 LLM 生成的 SQL；
- 未通过版本化契约、语义绑定、策略和预算的计划不得运行；
- 凭据只通过 secret reference 与最小权限工作负载身份获取；
- 敏感问题、Prompt、数据行、令牌和任意 SQL 不进入日志或遥测；
- 示例必须为合成数据；
- 未知操作符、字段、能力和契约版本默认拒绝。

安全政策不构成对当前软件生产可用性的声明。

